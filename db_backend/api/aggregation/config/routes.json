{
  "routes": [
    {
      "method": "GET",
      "path": "/aggregation/saleProfit",
      "handler": "aggregation.saleProfit",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/aggregation/topSaleCategory",
      "handler": "aggregation.topSaleCategory",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/aggregation/topRegions",
      "handler": "aggregation.topRegions",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/aggregation/topBusinesses",
      "handler": "aggregation.topBusinesses",
      "config": {
        "policies": []
      }
    }
  ]
}
